server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: ${CONTEXT_PATH:/api}

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/english-center-ms?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh
    username: root
    password: Lethinguyentrinh154@@

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  jackson:
    time-zone: ${TIMEZONE:Asia/Ho_Chi_Minh}